{
  "name": "hello-future-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "h3-js": "^4.1.0",
    "mapbox-gl": "^3.5.2",
    "react-map-gl": "^7.1.7"
  }
}
